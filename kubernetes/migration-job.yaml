apiVersion: batch/v1
kind: Job
metadata:
  name: db-migration:v1.5
  namespace: easyshop
spec:
  template:
    spec:
      containers:
      - name: migration:v1.5
        image: nileshfatfatwale/migration:v1.5
        imagePullPolicy: Always
        env:
        - name: MONGODB_URI
          value: "mongodb://mongodb-service:27017/easyshop"
      restartPolicy: OnFailure
